{
  "name": "to_delete_public_models",
  "version": "1.0.5",
  "description": "not availalbe",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build":"tsc",
    "prepare":"npm run build",
    "publishOnly":"npm test",
    "preversion": "npm run lint",
    "version": "npm run format && git add -A src",
    "postversion": "git push && git push --tag"
  },
  "keywords": ["test","toDelete"],
  "author": "myself",
  "license": "ISC",
  "files": ["dist/**/*"],
  "devDependencies": {
    "typescript": "^3.7.5"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/philipp985/_toDeleteModelsPublic.git"
  }
}
